# LibreOffice DOCX to PDF Converter (Docker + Flask)

This project provides a simple HTTP API that converts `.docx` files to `.pdf` using LibrPyPDF2eOffice in a single Docker container. It's based on `debian:bookworm-slim`, with PyPDF2 installed and a lightweight Flask server for conversion requests.

---
